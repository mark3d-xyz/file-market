import { styled } from '../../../../../../styles'

export const StyledPanelInfoItem = styled('div', {
  gap: '8px',
  display: 'flex',
  alignItems: 'center',
  '&:hover': {
    filter: 'brightness(1.2)',
  },
})
